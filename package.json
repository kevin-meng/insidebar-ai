{
  "name": "insidebar-ai",
  "version": "1.7.2",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage",
    "test:ui": "vitest --ui",
    "validate": "node scripts/validate-extension.mjs",
    "test:ci": "npm run validate && vitest --run"
  },
  "devDependencies": {
    "@vitest/ui": "^3.2.4",
    "happy-dom": "^20.0.2",
    "vitest": "^3.2.4"
  }
}
